name: Visual smoke StoryScro

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: storyscro-visual-${{ github.ref }}
  cancel-in-progress: true

jobs:
  visual:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.12'

      - name: Install Selenium
        run: python -m pip install --disable-pip-version-check selenium==4.35.0

      - name: Start static server
        run: |
          python -m http.server 4173 --bind 127.0.0.1 > /tmp/storyscro-server.log 2>&1 &
          echo $! > /tmp/storyscro-server.pid
          for i in {1..20}; do
            curl -fsS http://127.0.0.1:4173/v2/ >/dev/null && exit 0
            sleep 0.5
          done
          cat /tmp/storyscro-server.log
          exit 1

      - name: Capture and validate narrative scenes
        run: python scripts/visual_capture.py

      - name: Upload visual artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: storyscro-visual-${{ github.sha }}
          path: visual-artifacts/
          if-no-files-found: warn
          retention-days: 7
